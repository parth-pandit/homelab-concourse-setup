---
db_vm_type: 'large'   # the VM type for Postgres DB
db_persistent_disk_type: 'default'  # the disk type for persistence
web_vm_type: 'default'  # the VM type for Concourse web VM
worker_vm_type: 'large'  # the VM type for worker VMs
web_ip: '192.168.1.71'  # the IP address of Concourse web VM
db_ip: '192.168.1.72'  # the IP address reserved for the Postgres DB
deployment_name: concourse
network_name: default
external_host: ci.lab.cloudpandit.online
external_url: https://ci.lab.cloudpandit.online
main_team_oauth_users: ["ppandit"]
